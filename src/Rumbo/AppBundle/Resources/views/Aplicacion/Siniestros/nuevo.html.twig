{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Nuevo siniestro</h1>
    {{ form_start(form) }}
    {{ form_errors(form) }}
    {{ form_row(form.nombrerepresentado) }}
    {{ form_rest(form) }}

    <input type="submit" />
    {{ form_end(form) }}



    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('cliente') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
